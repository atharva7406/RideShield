fastapi==0.110.0
uvicorn==0.28.0
sqlalchemy==2.0.28
psycopg2-binary==2.9.9
redis==5.0.3
python-dotenv==1.0.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
pydantic==2.6.4
pydantic-settings==2.2.1
bcrypt==4.0.1
email-validator==2.3.0
python-multipart==0.0.32
requests==2.31.0
razorpay==2.0.1
alembic
httpx==0.27.2
pytest

# ML crash-detection inference (backend/app/services/ml_scoring_service.py).
# Deliberately only what predict.py/feature_extraction.py need at inference
# time — training-only deps (pandas, scikit-learn, pyarrow) stay confined
# to backend/ml_incident_engine/requirements.txt, not the production API.
numpy==1.26.4
xgboost==2.0.3

# Helmet-detection inference (backend/app/services/helmet_verification_service.py).
# onnxruntime/pillow kept for the original best.onnx classifier (currently
# unused — its training data was synthetic noise, never real photos, see
# helmet_detection_engine/config.py's docstring). opencv-python-headless
# is what verify_image() actually uses now: a classical Haar-cascade face
# detector bundled inside the package itself (no separate model download,
# no training data needed) — "no face detected in an otherwise valid
# selfie" is used as the full-face-helmet signal. Pinned <5 / with
# numpy==1.26.4 above deliberately — opencv-python-headless>=5 requires
# numpy>=2, which breaks xgboost/pandas/scikit-learn/pyarrow elsewhere in
# this file; do not upgrade one without checking the other.
onnxruntime==1.29.0
pillow==12.3.0
opencv-python-headless==4.9.0.80
