fastapi==0.110.0
uvicorn==0.28.0
sqlalchemy==2.0.28
psycopg2-binary==2.9.9
redis==5.0.3
python-dotenv==1.0.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
pydantic==2.6.4
pydantic-settings==2.2.1
bcrypt==4.0.1
email-validator==2.3.0
python-multipart==0.0.32
requests==2.31.0
razorpay==2.0.1
alembic
httpx==0.27.2
pytest

# ML crash-detection inference (backend/app/services/ml_scoring_service.py).
# Deliberately only what predict.py/feature_extraction.py need at inference
# time — training-only deps (pandas, scikit-learn, pyarrow) stay confined
# to backend/ml_incident_engine/requirements.txt, not the production API.
numpy==1.26.4
xgboost==2.0.3

# Helmet-detection inference (backend/app/services/helmet_verification_service.py).
# ONNX runtime only — deliberately NOT torch/ultralytics, matching the same
# "keep the production API's dependency footprint light" principle above;
# the exported best.onnx needs nothing beyond onnxruntime + Pillow to run.
onnxruntime==1.29.0
pillow==12.3.0
